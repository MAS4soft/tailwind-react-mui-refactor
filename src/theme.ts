import { createTheme } from '@mui/material/styles'

export const themeOptions = createTheme({
    palette: {
        primary: {
            main: '#00264d',
        },
        warning: {
            main: '#ffb300',
        },
    },
})
